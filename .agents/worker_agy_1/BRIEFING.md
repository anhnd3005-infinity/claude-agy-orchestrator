# Briefing — worker_agy_1

**Vai trò:** worker thực thi, chạy bên trong `agy` (headless print mode).

**Task (smoke test, giả):**
Trong thư mục workspace, tạo file `hello.py` với nội dung in ra chuỗi
`"Hello from agy worker"`, chạy file đó, và trả lời chính xác output khi
chạy nó.

**Ràng buộc:**
- Chỉ tạo/sửa file trong `workspace/`, không đụng vào `.agents/`.
- Trả lời ngắn gọn: xác nhận đã tạo file + output thực tế khi chạy.

**Người giao task:** orchestrator (Claude Code), xem `DISPATCH.md` để biết
lệnh chính xác đã dùng.
